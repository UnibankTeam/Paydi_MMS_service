package com.paydi.convert;

import org.springframework.stereotype.Service;

@Service
public class AccountInfoConverter {

	// public List<AccountInfoResponseDto> convertTimeStampToMillisecond(List<AccountInfoDto> loyalties) {
	// 	List<AccountInfoResponseDto> listAccountResponse = new ArrayList<>();
	// 	// convert
	// 	for (AccountInfoDto accountInfoDto : loyalties) {
	// 		AccountInfoResponseDto response = new AccountInfoResponseDto(accountInfoDto.getCif(),
	// 				accountInfoDto.getAccountNo(), accountInfoDto.getDisplayName(),
	// 				accountInfoDto.getActivatedOnDate() == null ? null : accountInfoDto.getActivatedOnDate().getTime(),
	// 				accountInfoDto.getAccountId(), accountInfoDto.getAccountBalance(),
	// 				accountInfoDto.getAccountActivatedOnDate() == null ? null
	// 						: accountInfoDto.getAccountActivatedOnDate().getTime(),
	// 				accountInfoDto.getAccountCurrencyCode(), accountInfoDto.getAccountTotalDeposits(),
	// 				accountInfoDto.getAccountTotalWithdrawals(),
	// 				accountInfoDto.getLastTransactionDate() == null ? null
	// 						: accountInfoDto.getLastTransactionDate().getTime(),
	// 				accountInfoDto.getAccountTypeShortName());

	// 		listAccountResponse.add(response);

	// 	}
	// 	return listAccountResponse;
	// }

	

}
